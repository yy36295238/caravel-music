# 轻量本地音乐播放器设计方案

> 已确认：支持 macOS 和 Windows；歌曲不到 1 万首；简单、轻量，保留基础播放、喜欢/收藏、分组和标签；支持多皮肤，首版内置深色红与清新绿。
> 两款静态交互原型已确认；已实现 Tauri 桌面版，打包及平台验收记录见 README.md。

## 1. 方案结论

做一个只有单窗口的本地 MP3 播放器：**添加音乐文件夹 → 搜索或选择歌曲 → 播放**。

歌曲保留在原目录，不复制、不搬移、不修改。应用只保存歌曲索引、收藏、分组、标签和上次播放状态，不需要账号、服务器或联网服务。

推荐 **Tauri 2 + JavaScript + 原生 HTML/CSS + SQLite**。使用系统 WebView 和原生音频播放能力，不随应用打包 Chromium；首版不引入 React、组件库或全局状态管理框架。

## 2. 首版功能

| 功能 | 具体行为 |
| --- | --- |
| 添加目录 | 支持多个本地目录，自动包含子目录，识别 `.mp3`，扩展名不区分大小写 |
| 歌曲列表 | 展示歌名、歌手、时长；标签没有歌名时显示文件名 |
| 搜索 | 按歌名、歌手、文件名搜索，支持中文子串和英文大小写不敏感 |
| 基础播放 | 播放/暂停、上一首/下一首、拖动进度、音量 |
| 播放模式 | 顺序播放、单曲循环、随机播放 |
| 喜欢/收藏 | 同一个爱心开关，支持收藏、取消收藏和“我喜欢的”筛选 |
| 分组 | 创建、重命名、删除分组；将歌曲加入或移出分组 |
| 自定义标签 | 创建、重命名、删除标签；给歌曲添加或移除多个标签并筛选 |
| 专辑 / 歌手 | 按本地索引生成浏览入口，点击进入歌曲列表；同名专辑按歌手区分 |
| 本地歌词 | 同名 LRC 优先，其次 ID3 USLT 内嵌文本；支持时间轴高亮、跟随和点击定位 |
| 多皮肤 | 内置深色红、清新绿，页面内即时切换并记住选择；后续可增加皮肤 |
| 手动刷新 | 新增或删除文件后，点击刷新更新音乐库 |
| 记住状态 | 记住目录、音量、模式、当前歌曲及播放进度；重启后暂停等待操作 |
| 目录管理 | 查看和移除已添加的目录，只移除索引来源，不删除磁盘文件 |

首版不做：封面墙、歌单编辑、在线歌词搜索、在线音乐、推荐、音效、桌面悬浮窗、托盘、主题商城、云同步和自动更新。

### 收藏、分组与标签的区别

- **喜欢/收藏**：先按同一功能设计，点亮爱心即收藏，统一在“我喜欢的”查看，不维护两套重复列表。
- **分组**：手动组织歌曲，例如“通勤”“工作”“睡前”。一首歌可加入多个分组，首版不做嵌套分组、自动规则或组内拖动排序。
- **标签**：描述歌曲属性，例如“粤语”“摇滚”“安静”。一首歌可有多个标签；这些是应用内自定义标签，与 MP3 的 ID3 元数据区分，不写回文件。
- 分组与标签名称去除首尾空白后不能为空，同类名称不能重复；删除前确认，只删除分类及关联，不删除歌曲、不影响收藏。
- 支持勾选多首歌曲后批量加入/移出分组、添加/移除标签；单曲操作使用相同入口，重复添加不产生重复关联。
- 收藏筛选、单个分组、标签和文字搜索可叠加；选中多个标签时匹配同时具有这些标签的歌曲。清空筛选恢复全部歌曲。

例：“晴天”可以同时被收藏、加入“通勤”和“睡前”分组，并带有“华语”“流行”标签。

## 3. 界面设计

保持一个主窗口，采用已确认的两款皮肤：左侧音乐导航、中央歌曲列表、底部播放器；深色红在宽屏显示右侧歌曲详情，清新绿采用宽列表。小屏收起侧栏，将筛选和换肤入口放到主区域。原型统一入口为 [prototypes/index.html](prototypes/index.html)。

下面示意核心功能区域，最终布局以已确认的皮肤为准。

```text
┌────────────────────────────────────────────────────────────────┐
│ 本地音乐       [ 搜索歌名 / 歌手 / 文件名 ]   添加目录  目录  刷新 │
├────────────────────────────────────────────────────────────────┤
│ 全部 / 我喜欢的     分组 ▾     标签 ▾     管理分类              │
│ 勾选后：加入分组 / 移出分组 / 添加标签 / 移除标签                │
│ 收藏  歌名                         歌手                  时长  │
│ ♥  ▶ 晴天                          周杰伦                4:29  │
│ ♡    七里香                        周杰伦                4:59  │
│   ...                                                          │
│                                                                │
│ 共 8,600 首                                  上一页  1  下一页 │
├────────────────────────────────────────────────────────────────┤
│ 晴天 · 周杰伦  ♥                                               │
│ 01:32 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 04:29  │
│        上一首     播放/暂停     下一首       顺序 ▾    音量 ━━━ │
└────────────────────────────────────────────────────────────────┘
```

- 使用系统字体和简单分隔线；深色红采用深色背景与红色强调，清新绿采用浅色背景与绿色强调，不增加装饰动画。
- 歌曲列表每页 100 首，避免一次渲染近万行；首版使用普通分页，不引入虚拟列表依赖。
- 复选框选中，点击歌曲名称或播放按钮播放；点播后按当前搜索、收藏、分组、标签筛选结果和排序建立内部播放队列，覆盖全部结果，不限于当前页。
- 改变搜索内容、收藏或分类筛选只影响列表，不打断播放，也不改变已经建立的队列。
- 列表和播放栏的爱心状态保持同步；“管理分类”用简单弹窗管理分组和标签，不增加独立页面。
- “目录”打开简单弹窗，列出已添加目录及移除按钮；刷新时就地显示扫描数量和失败数。
- 空格控制播放/暂停，但输入框和其他交互控件获得焦点时不拦截其按键。
- 控件支持键盘操作、清晰焦点和文字说明；窗口缩放时搜索框与歌曲列弹性调整。

### 多皮肤约定

- 初始提供“深色红”和“清新绿”，通过“更换皮肤”弹窗选择；首次默认深色红，以后恢复上次选择。
- 皮肤只控制颜色、字体、间距、布局和展示文案，不持有曲库、收藏、分组、标签或播放状态。
- 切换仅更新外观属性，不刷新页面、不重新创建播放器，不清空搜索、勾选或队列，也不丢失本次已选择的本地文件。
- 正式应用在 `src/main.js` 的 `skins` 列表注册 ID、名称、基础配色及文案，在 `src/styles.css` 中按 `data-theme` 定义外观；选择界面自动读取列表，不逐个硬编码切换分支。
- 后续增加内置皮肤，只需添加注册项及对应 CSS 样式；需要图片时随应用打包。沿用已有组件和业务事件，不复制播放器代码。
- 皮肤 ID 随应用设置保存；旧版本未保存或某款皮肤被移除时，回退默认皮肤。所有内置皮肤离线可用。
- 每增加一款皮肤，都验证文字对比度、键盘焦点、窄屏操作，以及换肤时播放不中断、分类与选择状态不变。

## 4. 技术选型与职责

| 部分 | 建议 | 职责 |
| --- | --- | --- |
| 桌面容器 | Tauri 2 | 窗口、系统目录选择、本地文件访问和打包 |
| 界面 | Vite + JavaScript + HTML/CSS | 单页面列表、搜索框和播放栏 |
| 播放 | 一个 HTMLAudioElement | MP3 解码、播放控制、进度和音量 |
| 标签解析 | Rust `lofty` | 读取歌名、歌手和时长，不自行解析 MP3 |
| 数据存储 | SQLite，Rust `rusqlite` | 保存目录、歌曲索引、收藏、分组、标签和播放状态 |

前端通过少量 Tauri 命令调用本地能力，例如添加目录、查询歌曲、刷新索引、更新收藏与分类、保存状态。Rust 端负责校验参数和访问文件，耗时扫描放在后台任务，避免阻塞界面。

不启动本地 HTTP 服务，不安装数据库服务，不引入搜索引擎，也不构建通用插件或多层业务框架。

### 跨平台边界

- macOS 使用系统 WKWebView；Windows 使用 WebView2。Windows 安装器需检测运行时，缺失时安装；若要求首次安装也完全离线，需另行提供包含运行时的离线安装方案。
- 应用使用期间核心功能完全离线。Tauri 不捆绑完整浏览器通常有利于减小包体，但最终安装大小、内存占用以打包实测为准。
- macOS 提供 Apple Silicon 和 Intel 构建，Windows 首版提供 x64 安装包；具体最低系统版本随所选 Tauri/WebView 版本确认。
- 文件路径使用平台路径 API 处理，不硬编码分隔符或数据目录，不统一转小写。
- macOS 关闭窗口后继续播放，通过 Dock 重开；Windows 关闭窗口即退出，最小化继续播放。菜单“退出”结束应用。
- 两个平台分别验证实际安装包的播放、拖动定位、目录访问和中文路径。公开分发时再准备对应签名和 macOS 公证。

## 5. 文件导入与本地索引

### 导入流程

1. 通过系统对话框选择目录，校验访问权限并保存真实路径。
2. 后台遍历 MP3，默认不跟随符号链接，避免循环与越界读取。
3. 读取文件大小、修改时间和基本标签，分批保存；显示已处理数量和异常数量。
4. 已完成的歌曲可先查看和播放，整个过程不读取全部音频到内存。
5. 单文件失败不终止扫描；无标签使用文件名，标签损坏时仍允许尝试播放。

### 刷新规则

- 启动时直接读取已有索引，不自动全量扫描；需要更新时手动刷新。
- 相同真实路径只收录一次；文件大小和修改时间不变时跳过重新解析。
- 首版不自动去重内容相同但路径不同的文件，不计算全量音频哈希。
- 为简化目录归属，添加已存在目录及其子目录时提示“已包含”；若新目录包含已有目录，提示先移除旧目录再添加。
- 只有目录完整扫描成功，才把未发现的文件标记为缺失；权限错误、扫描中断和移动硬盘离线不能触发批量缺失判定。
- 移动硬盘离线时保留索引，提示目录不可用；盘符、挂载位置变化或文件移动后，由用户重新添加目录；首版不自动迁移不同路径歌曲的收藏和分类关联，原记录保留为不可用。
- 文件大小与修改时间均未变化时，外部标签修改可能无法被识别；首版需确保文件修改时间变化后再刷新；不增加 ID3 编辑功能。

### 搜索规则

不到 1 万首，将索引元数据读入界面内存，使用字面子串匹配和分类交集过滤，不预建全文索引。输入停顿 160 ms 后搜索；不发送搜索请求，也不将音频读入索引。后续实测规模超过这一边界再下推 SQL 查询。

列表按歌名排序，并以稳定歌曲 ID 作为同名歌曲的次级排序条件；每页 100 首。播放队列仅持有歌曲 ID，不把全库音频或完整标签对象加载进内存。

## 6. 播放规则

| 场景 | 行为 |
| --- | --- |
| 顺序播放 | 按队列播放，最后一首结束后停止 |
| 单曲循环 | 自然播放结束后重复当前歌曲，手动下一首仍切换歌曲 |
| 随机播放 | 先打乱队列再依次播放，一轮内不重复；上一首返回实际播放历史 |
| 点播不可用文件 | 显示错误，不擅自播放别的歌曲 |
| 自动连播遇到错误 | 跳过并提示；本轮没有可播放歌曲时停止，避免死循环 |
| 快速连续切歌 | 忽略旧歌曲的加载结果，保证界面和声音对应 |
| 恢复进度 | 音频元数据加载成功后设置进度，恢复为暂停状态 |

全应用只维护一个音频元素，进度与播放状态以真实媒体事件为准。定期保存进度，暂停、切歌和退出时补存；只在队列变化时保存队列，避免频繁重写。

本地音频通过受限 `music` 协议按歌曲 ID 访问，不转换为 Base64。Range 请求单次最多读取 1 MiB；非 Range 请求回退整文件响应，超大音频内存优化不在首版范围。实现阶段首先验证 macOS/Windows 的 MP3 加载、字节范围请求及拖动进度；不能假设不同 WebView 的媒体行为完全一致。

## 7. 最小数据结构

| 表 | 主要字段 | 用途 |
| --- | --- | --- |
| `directories` | `id`、`path`、`active`、`status` | 目录来源，路径唯一，移除时停用 |
| `tracks` | `id`、`directory_id`、`path`、`filename`、`title`、`artist`、`album`、`seconds`、`size`、`modified`、`available`、`favorite`、`metadata_error`、`last_seen` | 歌曲索引，UUID 字符串主键与唯一路径 |
| `categories` | `kind`、`name` | 分组和标签共享结构，以类型与名称组成主键 |
| `track_categories` | `track_id`、`kind`、`name` | 多对多关联，联合唯一，分类重命名和删除级联 |
| `app_state` | `key`、`value` | 播放设置与队列分别存储，避免频繁重写大队列 |

SQLite 文件存入系统应用数据目录。写入使用事务；数据库升级前备份，不覆盖失败迁移前的数据。扫描只更新文件元数据，不覆盖收藏或分类关联；收藏、分组和标签是不能通过扫描重建的用户数据，必须持久保存。批量分类修改使用事务，关联表启用外键和唯一约束；重复关联直接忽略。

移除目录时先确认，再停用该目录并更新队列；如果当前歌曲属于该目录则停止播放。保留歌曲 ID、收藏、分组和标签，在收藏和分类中显示为不可用；重新添加相同路径后复用原记录恢复关联。文件缺失和硬盘离线同样不得清除这些用户数据。以上操作不删除或修改原始 MP3。

## 8. 安全与错误反馈

- 只允许访问用户主动添加的目录；校验真实路径边界，阻止路径穿越及符号链接越界。
- Tauri 权限按最小范围配置，不开放任意文件访问或任意命令执行；新加目录在用户选择后扩展访问范围，移除后撤销。
- 标签作为纯文本显示，界面不加载远程内容；文件名、音乐标签和播放记录不上传。
- 没有歌曲、搜索无结果、文件缺失、目录无权限分别显示明确文案和对应操作。
- 日志只记录扫描摘要、收藏及分类写入摘要和必要错误，限制文件大小，不逐首记录成功日志，不输出完整标签或音频内容。

## 9. 开发顺序与验收

| 阶段 | 交付 | 验证方式 |
| --- | --- | --- |
| 1. 验证跨平台播放 | 最小 Tauri 窗口、选择 MP3、播放/暂停、进度和音量 | 在 macOS 与 Windows 的安装包中播放真实 MP3，验证中文路径及可变码率定位 |
| 2. 完成曲库 | 添加目录、扫描、SQLite、搜索、分页列表 | 用接近 1 万首的样本验证扫描、刷新、重复目录处理及重启读取 |
| 3. 完成日常使用 | 播放模式、状态恢复、目录管理、收藏、分组、标签及两款皮肤 | 验证即时换肤和偏好恢复、队列稳定、分类增删改与批量操作、组合筛选、重复关联防护及重启持久化 |
| 4. 打包交付 | macOS 与 Windows 安装包 | 验证断网运行、运行时依赖、权限拒绝、外接盘断连和文件缺失 |

收藏与分类还需验证：移除目录、文件缺失、重新扫描不会丢失数据；同路径重新导入可以恢复；删除分组或标签不影响文件及收藏；写入失败时提示错误，界面不保留虚假的成功状态。

性能验收目标：以 SSD、接近 1 万首样本为基线，已建库启动到可操作争取在 2 秒内；搜索停止输入后首屏结果争取在 300 ms 内呈现。扫描期间可以正常操作和播放。

包体、空闲内存、扫描耗时应在两平台记录实测值，不将“轻量”写成未经验证的固定数字。优先保证单窗口、少依赖、按需读取和不常驻后台扫描。

交付标准：两平台均能稳定完成“添加目录、查找歌曲、播放、收藏、分组、打标签、重启继续使用”，不改动用户的音乐文件。

## 10. 已新增：专辑、歌手与歌词

专辑与歌手由已有索引聚合，无需迁移数据库。按歌手与专辑名共同识别专辑，不自动拆分合作歌手或联网补齐标签。分类浏览与播放队列分离，切换页面不会改变正在播放的顺序。

歌词通过受限命令按歌曲 ID 按需读取，依次尝试同目录同名 LRC 和 MP3 ID3 USLT。支持 UTF-8、UTF-16 BOM 与 GBK；1 MiB 和 10,000 行 / 时间点为输入上限。带时间轴的歌词高亮并跟随播放，可点击定位；纯文本全文显示。首次没有歌词时提供文件放置指引，修改后手动重新读取。两款皮肤共用一个非模态歌词面板。

不修改原始音乐和歌词文件，不把整库歌词存入索引；暂不提供网络歌词或逐字 / SYLT 同步歌词。


## 菜单栏播放器与设置补充

- macOS：原生模板菜单栏图标随播放状态律动，悬浮展示当前歌曲及上一首、播放 / 暂停、下一首；点击固定浮层，移开悬浮区域延迟收起。支持打开主窗口与 Esc 收起。
- 浮层控制主窗口的唯一音频实例，关闭主窗口继续播放；不另建队列或改写音乐索引。Windows 保持主窗口操作。
- 设置集中承载外观皮肤和标签管理。分组保留侧栏入口，歌曲列表继续提供标签筛选及分类编辑。
- 深色红 / 清新绿共用主题变量，菜单栏浮层随主窗口换肤；尊重系统减少动态效果设置，不做频谱采样。


### 浮层与主界面歌词调整

- 菜单栏采用 248 × 136pt 小卡片、16pt 圆角和透明阴影边缘，浮层在触发屏幕的图标中心下方固定展开。
- 原生定位统一使用 AppKit 点坐标，避免主屏和扩展屏不同缩放比例造成偏移；已展开时不随鼠标或图标动画重复定位。
- 主界面默认展示当前歌曲歌词：深色宽布局位于右侧，浅色或窄布局位于曲库上方；完整歌词面板继续可展开。
- 播放过程中平滑跟随歌词，高亮不改变字重；用户跳转时间或系统减少动态效果时立即定位。两处歌词共用当前歌曲解析结果，手动浏览独立暂停跟随。
